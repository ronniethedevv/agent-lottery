{
  "name": "@agent-lottery/circuits",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "clean": "rimraf build keys",
    "test": "node --experimental-vm-modules node_modules/.bin/jest"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.4"
  },
  "devDependencies": {
    "circom_tester": "^0.0.20",
    "jest": "^29.7.0",
    "rimraf": "^5.0.5"
  }
}
